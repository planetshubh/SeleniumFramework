package com.core.enums;

/**
 * Enums to restrict the users to choose an appropriate waiting strategy before operating an element rather than typing any value from keyboard.
 */

public enum WaitStrategy {
    CLICKABLE,
    PRESENCE,
    VISIBLE
}

